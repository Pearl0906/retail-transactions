resources:
  jobs:
    marketing_bronze_ingestion:
      name: marketing_bronze_ingestion

      tasks:
        - task_key: bronze_ingestion

          notebook_task:
            notebook_path: ../notebooks/bronze/bronze_ingestion.ipynb
            warehouse_id: ${var.warehouse_id}